<%= form_with(model: promise, local: true) do |form| %>
  <% if promise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(promise.errors.count, "error") %> prohibited this promise from being saved:</h2>

      <ul>
      <% promise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, id: :promise_amount %>
  </div>

  <div class="field">
    <%= form.label :estimated_delivery_date %>
    <%= form.date_select :estimated_delivery_date, id: :promise_estimated_delivery_date %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :promise_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
