
<p id="notice"><%= notice %></p>



<p id="notice"><%= notice %></p>
<% if current_user %>
  <h1>Projects</h1>
  <% if current_user.role != "admin" %>
    <% @projects.each do |project| %>
    <div class="container-fluid" style="margin-left: 0.5%">
      <div class="row">



          <div class="col-md-12">

            <div class="jumbotron" style="background: lightgray">
              <div class="caption">
                <h2><%= project.title %></h2>
                <div class="row">
                  <div class="col-md-3 ">

                    <%= image_tag project.image.url(:thumb) %>
                  </div>
                  <div class="col-md-4">
                    <div>Fund Goal : <%= project.goal_amount %> USD</div>
                    Total Fund Collected
                    <div class="progress">
                      <div class="progress-bar" role="progressbar" aria-valuenow="60" aria-valuemin="0" aria-valuemax="100" style="width: 50%;">
                        50%
                      </div>
                    </div>
                    <div>Days to go:  <%= project.days_to_go %></div>
  <!--                  <div>Category: <%#= project.category %></div>-->
                    <div href="#" class="btn btn-primary" role="button" style="color: white">Fund</div>
                    <div class="btn btn-success">
                      <td style="color: white"><%= link_to 'Show', project %></td>
                    </div>
                    <div class="btn btn-success">
                      <td style="color: white">
                        <%= link_to "Add to wishlist", addproject_path(:project_id => project.id) %></td>


                      <%#= wishlist = Wishlist.find(current_user.id)%>

                      <%#=if wishlist.id == "1" %>
                        <%# link_to "Add to wishlist", new_wishlist_path %>
                      <%#= end  %>
                      <%#= wishlist = Wishlist.find(current_user.id)%>
                      <%# link_to "Add to wishlist", new_wishlist_project_path(:project_id => project.id, :wishlist_id => wishlist.id) %>
                    </div>
                  </div>
                  <div class="col-md-3">
                    <h5>Markdown</h5>
                    <blockquote><%= project.marckdown %></blockquote>
                  </div>
                  </p>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>


    <br>

    <%= link_to 'New Project', new_project_path %>
    <%= link_to 'Back Home', home_index_path %>
  <% else %>
    <div class="col-md-2"></div>
    <div class="col-md-4">
      <h1>All Projects</h1>

      <table class="table">
        <thead>
        <tr>
          <th>Title</th>
        </tr>
        </thead>
        <tbody>
        <% @projects.each do |project| %>
          <tr>
            <td><%= project.title %></td>
            <td class="btn btn-success"><%= link_to 'Show', project %></td>
            <td class="btn btn-info"<%= link_to 'Edit', edit_project_path(project) %></td>
            <td class="btn btn-danger">Delete</td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  <% end %>
<% else %>
  <% @projects.each do |project| %>
    <div class="container-fluid" style="margin-left: 0.5%">
      <div class="row">



        <div class="col-md-12">

          <div class="jumbotron" style="background: lightgray">
            <div class="caption">
              <h2><%= project.title %></h2>
              <div class="row">
                <div class="col-md-3 ">

                  <%= image_tag project.image.url(:thumb) %>
                </div>
                <div class="col-md-4">
                  <div>Fund Goal : <%= project.goal_amount %> USD</div>
                  Total Fund Collected
                  <div class="progress">
                    <div class="progress-bar" role="progressbar" aria-valuenow="60" aria-valuemin="0" aria-valuemax="100" style="width: 50%;">
                      50%
                    </div>
                  </div>
                  <div>Days to go:  <%= project.days_to_go %></div>
                  <!--                  <div>Category: <%#= project.category %></div>-->


                </div>
                <div class="col-md-3">
                  <h5>Markdown</h5>
                  <blockquote><%= project.marckdown %></blockquote>
                </div>
                </p>
              </div>
            </div>
          </div>
        </div>
  <% end %>
  </div>
  </div>


  <br>

  <%= link_to 'New Project', new_project_path %>
  <%= link_to 'Back Home', home_index_path %>
<% end %>

