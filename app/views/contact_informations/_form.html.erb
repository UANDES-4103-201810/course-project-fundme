<%= form_with(model: contact_information, local: true) do |form| %>
  <% if contact_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_information.errors.count, "error") %> prohibited this contact_information from being saved:</h2>

      <ul>
      <% contact_information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :contact_information_address %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number, id: :contact_information_phone_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
