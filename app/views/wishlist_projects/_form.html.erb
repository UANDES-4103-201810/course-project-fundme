<%= form_with(model: wishlist_project, local: true) do |form| %>
  <% if wishlist_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wishlist_project.errors.count, "error") %> prohibited this wishlist_project from being saved:</h2>

      <ul>
      <% wishlist_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :wishlist_id %>
    <%= form.text_field :wishlist_id, id: :wishlist_project_wishlist_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :wishlist_project_project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
